// concat
// -> concatenação de arrays
// -> não altera a array original, tem que salvar em uma nova const

const meninas = ['Samanta', 'Sandra', 'Sâmia'];
const meninos = ['Sandro', 'Samuel', 'Sílvio'];

const mix = meninas.concat(meninos);
console.log(mix);


